# Environment variables
python-dotenv==1.1.1

# LangGraph CLI and API (needed for deployment)
langgraph-cli>=0.4.0
langgraph-api>=0.4.7
langgraph-checkpoint>=2.1.1
langgraph-runtime-inmem>=0.9.0
langgraph-sdk>=0.2.4

# Core Python packages (not in setup.py)
pydantic_core>=2.33.2
starlette>=0.47.3
PyYAML>=6.0.2
packaging>=25.0

# Anthropic and OpenAI
anthropic>=0.64.0
openai>=1.102.0

# Additional utilities
requests>=2.32.5
click>=8.2.1
structlog>=25.4.0
orjson>=3.11.3

# Core dependencies are now in setup.py to avoid duplication